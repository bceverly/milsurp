-r requirements.txt

# Testing
pytest>=9.1.1
pytest-cov>=7.1.0
# Starlette's TestClient transport. Starlette >= 1.0 imports httpx2 and
# emits a deprecation warning if it has to fall back to httpx — and this
# project runs pytest with `filterwarnings = error`, so the fallback is a
# hard failure. Both are listed because requirements.txt allows the whole
# fastapi>=0.115,<1.0 range, which spans the change.
httpx2>=2.13.0
httpx>=0.28.1            # fallback for pre-1.0 Starlette
responses>=0.26.3          # stub HTTP for scraper tests

# Linting and formatting
ruff>=0.16.8
black>=26.5.1
mypy>=2.3.1
bandit[toml]>=1.9.4      # security-focused static analysis
types-PyYAML>=6.0.12.20260906
types-requests>=2.33.0.20260906

# An independent implementation of RFC 8188/8291, used by the web push tests to
# decrypt what our own encryption produced. It is the cross-check that makes
# those tests worth anything: a round trip against ourselves would agree with
# whatever we had misread. Never shipped -- debian/rules deletes this file from
# the package.
http-ece>=1.2.1
