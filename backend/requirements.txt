# Web framework and server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
python-multipart>=0.0.20
email-validator>=2.2

# Database
SQLAlchemy>=2.0.36,<3.0
alembic>=1.14,<2.0

# Configuration
PyYAML>=6.0.2

# Security: Argon2id password hashing and JWT sessions
argon2-cffi>=23.1
PyJWT>=2.10

# Image processing (thumbnail generation during scans)
Pillow>=11.0

# Scraping
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.3
selenium>=4.27

# OCR, for vendors that publish a scanned flyer instead of a catalog
# (Hunter's Lodge). This is only the Python binding: it needs the `tesseract`
# binary, which `make install-dev` installs and which a site's scraper checks
# for before using. A machine without it runs everything else normally.
pytesseract>=0.3.13
